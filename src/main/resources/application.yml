server:
  port: ${port:8888}

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/sa-group-05/cloud-config-server-book-author
          default-label: master
          bootstrap: false
          clone-on-start: true

